# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #364 — feat(d52): migration 0029 — outbound_sync_state admits 'pending_update' (R4/R5) [SQL-TO-APPLY]

- Label: `needs-directional-ruling`
- Head SHA: `4548dea28e34f592348cab7048712a33188233fc`
- **SQL TO APPLY: yes — supabase/migrations changed. Love applies manually via the Supabase SQL editor.**

ORCH-PARK

**What this PR does (plain English):** Adds one database change: the `tasks.outbound_sync_state` column (the per-task "is SuiteFleet in sync with us?" marker) learns a sixth value, **`pending_update`** — "an operator changed this delivery's address and the update is on its way to SuiteFleet." Without it, the calendar can't show the in-flight state for the new address-override pushes (R4/R5). Includes the schema self-test.

**What it touches:** `supabase/migrations/0029_*.sql` + one integration spec. No code paths change in this PR.

**Reviewer verdict (1 line):** REQUEST_CHANGES — SQL is technically correct, but the authority for it (Day-52 OQ-1 ruling "(a)" from plan #335) has no merged repo record (#335 closed-unmerged), so the reviewer flags it as a Love-only directional confirmation; plus migrations always park.

**Why it parked under `needs-directional-ruling`:** Love's Day-52 rulings (R4/R5 build + OQ-1 'pending_update' enum) were delivered in the overnight build tasking and are not yet encoded in the repo. Per the §9 precedent (memo `memory/decision_workflow_autonomy_single_checkin.md`), Love's named clearance of this PR IS the verification of that ruling. A decision memo encoding the Day-52 rulings rides in the PR-4 branch (stacked on this one) so the record lands with the lane.

**SQL TO APPLY: yes** — `supabase/migrations/0029_tasks_outbound_sync_state_pending_update.sql`, via Supabase SQL editor, on Love's explicit named authorization, **BEFORE** the dependent R4/R5 code-PRs (stacked on this branch) promote. Not applied anywhere except the local throwaway test DB.

**Clearance order:** #364 (this) → PR-4 (R4 code) → PR-5 (R5 code).

## PR #368 — feat(d52): R5 — forward address override backfills in-horizon tasks + fans out SF updates (PR-5)

- Label: `parked-t3`
- Head SHA: `1575a7a0301663ae7896ba1019036742d1df2953`

ORCH-PARK

**What this PR does (plain English):** "Change address (from this delivery onwards)" now actually moves the upcoming deliveries. Before: the next ~14 days of deliveries silently kept the old address and SuiteFleet was never told — only far-future deliveries picked up the change. After: every upcoming delivery on this subscription is re-pointed at the new address, SuiteFleet gets an update for each one already dispatched, and the operator first confirms via an inline popup with the exact ruled wording ("Are you sure you want to update the address for all future tasks on this subscription?"). Other subscriptions for the same consignee are untouched (ruled). Also bumps the product brief to v1.18 (the ruled documentation amendment) and fixes a test-infrastructure gap: two existing UI test files were silently never running; they now run and pass (test gate grew 1984 → 2001).

**What it touches:** exception service + tasks repository (data path), one new audit event, the calendar popover confirm dialog (new small component), the product brief, vitest config. 9 files.

**Reviewer verdict (1 line):** APPROVE, round 1 — no Love-triggers flagged.

**Why parked:** all code parks for Love (Shape-3 v1).

**SQL TO APPLY: no** (this PR) — depends on PR #364's migration 0029 via PR #367. **Clearance order: #364 (apply 0029 first) → #367 → this (#368).**

**CI note:** stacked PR — CI fires when the base auto-retargets to main after #367 merges; verify green there before merging. Local: unit 2001/2001, integration 474/474 on the local 0029-provisioned Postgres (production untouched).

## PR #367 — feat(d52): R4 — one-off address override backfills task + pushes SF update (PR-4)

- Label: `parked-t3`
- Head SHA: `18c2a6d74b45b7057255d2428a8cdea84830c649`

ORCH-PARK

**What this PR does (plain English):** "Change address (this delivery only)" on the calendar now actually changes the delivery. Before: it wrote a note in the database and nothing else — the driver still went to the old address. After: the delivery task itself is re-pointed at the new address, the new address is sent to SuiteFleet immediately (with the address details built safely on the server), and the calendar shows a "Sending to SuiteFleet" badge until SF confirms. Bonus fold (ruled option B): the /tasks page address edit now also pushes to SuiteFleet — its old "will reflect on the next scheduled push pass" small-print is retired.

**What it touches:** exception service + tasks repository/service (data path), the SF update queue routes, the calendar popover badge, the /tasks edit action copy, one new audit event type. 18 files, ~1,090 lines (mostly tests + the in-branch Day-52 rulings memo).

**Reviewer verdict (1 line):** APPROVE, round 1 — no Love-triggers flagged by the reviewer.

**Why parked:** all code parks for Love (Shape-3 v1; park lane regardless of verdict).

**SQL TO APPLY: no** (this PR) — but it DEPENDS on PR #364's migration 0029. **Clearance order: #364 (apply 0029 first) → this (#367) → PR-5.** Promoting this before 0029 is applied breaks every address override with a CHECK violation.

**Tests:** unit 1984/1984; integration 469/469 (77 files) on a local 0029-provisioned Postgres — production untouched.

## PR #365 — fix(d52): inbound TZ — edit-apply converts SF UTC window times → Dubai-local (+ UAT proving-pass tooling)

- Label: `parked-t3`
- Head SHA: `9f942415835ea94c8e4211cca152ea79a2f2ffb4`

ORCH-PARK

**What this PR does (plain English):** Fixes the Day-52 §D inbound timezone bug. When SuiteFleet reflected a task edit back to Planner, Planner wrote SF's UTC delivery-window times straight onto the calendar as if they were Dubai times, shifting the window 4 hours earlier (your 06:00–09:00 window on Roudy M showed as 02:00–05:00). The fix converts SF's UTC times to Dubai time on the way in, using the same shared converter the status-webhook path already uses — the mirror image of the outbound fix you ruled in PR #307. An SF echo of an unchanged window is now correctly recognized as "nothing changed" instead of being applied as a bogus edit. Also commits the three Day-52 proving-pass UAT scripts as reusable tooling (second commit).

**What it touches:** one source file (the inbound edit-webhook applier), two test files (assertions updated to the corrected contract + 3 new regression tests that fail pre-fix on real Postgres), three new manual scripts under `scripts/`. **No migration — code only.** Local signal: unit 1983/1983, integration 463/463, tsc + eslint clean.

**Reviewer's verdict (one line):** APPROVE round 1 (opus) — "the fix does exactly what it claims, touches only what it claims, and is a true mirror-image of the already-ruled outbound conversion"; one non-blocking heads-up that `scripts/uat-leg.mjs` performs live production writes when run manually.

**SQL TO APPLY: no.**

**Separate flag for Love (not this PR):** the Layer-1 path-gate script fails OPEN on a GitHub API network error — a `gh api` failure leaves the file list empty and it prints AUTO_MERGE_ELIGIBLE for a code PR (observed live tonight; re-run classified PARK correctly). Layer 2 (the Action) re-gates server-side so code cannot actually merge, but the Layer-1 defect is filed as `memory/followup_path_gate_fail_open_on_api_error.md` riding the queue-doc docs PR.


# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #494 — docs(d54): UAT GREEN v2 — live-UI walk complete, all legs PASS

- Label: `parked-t3`
- Head SHA: `1a52502faf3cfb04b7b4a328f9afe67e6ca3f056`

ORCH-PARK (Session A, firing item 5 — round-2 dispute trail in place)

**Plain English:** this PR records that the live-UI walk is DONE and every leg PASSED — the product walk itself is clean and is not in dispute. The reviewer's round-2 objection is one of wording consistency: the sheet's top line says UAT GREEN v2 is *unconditional*, while the preflight gate-8 QUERY fix (#493 — cosmetic to the readiness gate, the underlying data invariant is verified true) is still parked, so the reviewer reads the declaration as leaning on an unmerged repair. Two rounds used; parking per discipline rather than re-prompting.

**Resolves with ONE of your lines:**
- "#493 cleared." → the gate-8 fix merges → #494's claims become literally true as written → clear #494.
- Or rule the wording ("GREEN stands; gate-8 fix may land after") → I align the §END text and it rides.

**Scope:** docs only. **SQL TO APPLY: no.**

## PR #493 — fix(d54): preflight gate 8 — tenant scope + exact name + deterministic order

- Label: `parked-t2`
- Head SHA: `83f51da1e59c2d9c39d548362b04278cab7ca8f8`

ORCH-PARK (Session A, Day-54 firing item 4)

**Plain English:** the pre-demo readiness check had one gate that could cry wolf — it looked for "Sarah Khouri" across EVERY tenant (including hundreds of test clones) and trusted whichever row came back first. This fix points it at the real demo Sarah on the demo tenant only, with a deterministic order. Watched failing before the fix (9/10), passing after (**10/10**).

**Scope:** one script (`scripts/demo-preflight.mjs`), read-only SELECT. No app code, no DB writes. **SQL TO APPLY: no.**

**Reviewer:** ORCH-VERDICT APPROVE r1 at pinned head `83f51da` (body-read incl. seed + fixture cross-checks). Parks per tier shape (scripts are off the docs allowlist).

**Clears on:** your one-line clear.


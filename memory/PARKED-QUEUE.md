# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #398 — docs(d53): runbook sync — three-pair scaling rules (parks; companion to #397)

- Label: `needs-directional-ruling`
- Head SHA: `59a49b6a5dba6a304bd75fbe745d964cb2e71a06`

ORCH-PARK — PR #398, head `59a49b6`. Parked `needs-directional-ruling` per reviewer Standing Order 5 (no revision attempt).

**What this PR does:** adds one section to the orchestration runbook (`scripts/orchestration/RUNBOOK.md`) recording your three-pair scaling rules: (1) do-not-touch lists in every dispatch; (2) one EOD owner per day — Session A; (3) brief version bumps dispatch-assigned, never self-assigned. Nothing else touched. Companion to the decision memo in #397.

**Reviewer verdict (r1, opus): REQUEST_CHANGES — directional, not a defect.** The reviewer found the change mechanically clean and faithful to the memo, but flagged that a new operating policy for how the build process scales is yours to confirm — your ruling lives in Session C's dispatch, which the reviewer's separate context cannot see (same shape as the #394 precedent). It also noted the memo file referenced here lives in #397, still open — resolved automatically once #397 merges first.

**Clears on:** your one-line confirm that the three-pair scaling ruling quoted in #397 is yours → #397 merges (docs lane) → this merges after it.

**SQL TO APPLY:** none.

## PR #400 — docs(d53): runbook — firing-as-clearance mechanic (autonomy/clearance section)

- Label: `parked-t3`
- Head SHA: `194256ffd3e225d14d7145d9f39c53c23b2df247`

ORCH-PARK — PR #400, head `194256f`. Parked `parked-t3` (off-allowlist by design; reviewer-clean).

**What it is:** folds Love's 2026-06-11 firing-as-clearance amendment into `scripts/orchestration/RUNBOOK.md` (the autonomy/clearance section).

**Reviewer r1 (sonnet): APPROVE.** The text faithfully encodes Love's verbatim amendment; the two-party seam is explicitly preserved; carve-outs (live DB/SQL named authorization, new spend, open reviewer-surfaced decisions) are intact.

**Why it parks (not a defect):** it edits `scripts/orchestration/RUNBOOK.md`, which is OUTSIDE the path-gate allowlist (`memory/**, docs/**, tasks/**, root *.md`). By construction an off-allowlist PR cannot auto-merge — it parks. This is the intended behaviour Love named: "that edit is off the docs allowlist, so it PARKS."

**SQL TO APPLY:** none. **CI:** docs/prose only.

**Clears on:** Love's next dispatch firing that names #400 as cleared-by-firing (the very mechanic this PR documents) → builder merges via gh api (admin route, off-allowlist) and it rides the routine promote. No pre-UAT promote.

## PR #396 — fix(admin): merchant detail AUTH METHOD shows effective method (override-aware)

- Label: `parked-t2`
- Head SHA: `399c0d960339dc62e1320578150748d91dbca374`

ORCH-PARK — PR #396, head `399c0d960339dc62e1320578150748d91dbca374`. Parked `parked-t2` (Shape-3: all code parks).

**What it does:** merchant detail page AUTH METHOD row now shows the EFFECTIVE method (override ?? region default), annotated "(merchant override)" like the credentials page — Love found Demo Bistro's api_key flip invisible there (still read OAUTH off the region's raw method). DTO projects `suitefleet_auth_method_override`; new pure helper mirrors the resolver's selection. Surface sweep: this was the only stale surface (regions pages show their own default correctly; merchants list has no auth column; credentials page already effective-aware).

**Verification:** RED-first — 3 unit + 2 real-Postgres cases watched fail, then green. Full suites 2015 unit / 485 integration, `tsc` 0. Path-gate PARK ✔, reviewer APPROVE round 1 ✔.

**SQL TO APPLY:** none (0030 already applied Day-54 AM).

**Clears on:** Love's one-line confirm + CI green at the head SHA above → merge via the admin route → rides the next routine promote (UI-only; does not gate UAT).


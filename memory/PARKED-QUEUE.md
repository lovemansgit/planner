# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #437 — docs(d53): Tier-2 big-number unification — cross-lane handoff (4b fix is Session A's lane)

- Label: `needs-directional-ruling`
- Head SHA: `831774fdbecc455c9505913f2ffc25b7308bcc53`

ORCH-PARK

**What it is:** the held Tier-2 "big-number unification" — filed as a routing handoff, not a code PR, because there's **nothing to change in Session B's lane.** `/tasks` and `/consignees` big-count headers already use the canonical treatment; the single odd-one-out is the **subscriptions** list page (a bigger font), which is **Session A's area**.

**Why it parks for you:** it's a one-line cosmetic fix sitting in another session's lane, so rather than reach across, here's the one-liner — (1) **hand it to Session A** (recommended), (2) let **Session B** make the one-line exception, or (3) **defer** to the future shared hero-number component (the audit's "do it once, everywhere" item).

**Reviewer:** ORCH-VERDICT APPROVE (r1). No DB. **SQL TO APPLY: no.** No brief bump.

**Pairs with:** #435 (H4 token refactor + /tasks polish, parked-t2) — same Tier-2 wave.

## PR #432 — docs(d53): PLAN — one-screen subscriber onboarding (click-reduction)

- Label: `needs-directional-ruling`
- Head SHA: `5588d63818e8b4d7ffa56a2957632d4726802dd7`

ORCH-PARK

**What it is:** a PLAN (no code) to collapse new-subscriber onboarding from two routes (create consignee, then create subscription) into one combined screen.

**⚠️ Reviewer caught a brief conflict — please read:** this plan points the **opposite way** to a settled decision. Brief amendment **v1.12** explicitly **decoupled** consignee creation from subscription creation (it removed the old wizard in favour of a flat consignee form + separate "New subscription" CTA on the Overview page). This one-screen onboarding plan would effectively **re-couple** them. So this isn't a neutral plan — it asks you to **reverse v1.12**, or to confirm v1.12 still stands and drop this.

**Why it parks for you:** three one-liner questions — (1) approve a one-screen consignee + first-subscription form (reversing v1.12)? (2) keep `/subscriptions/new` standalone for existing consignees, or fully merge? (3) add a new UAT step for it, or post-UAT? If v1.12 stands, the answer is simply "keep decoupled — drop this plan."

**Touches at build time:** shared lane (`consignees/new` + `subscriptions/new`). No DB. **SQL TO APPLY: no.**

## PR #431 — docs(d53): PLAN — /tasks filter-stack collapse (click-reduction)

- Label: `needs-directional-ruling`
- Head SHA: `70b39c6a7ab5d9f8558a6309c3b267987ac03570`

ORCH-PARK

**What it is:** a PLAN (no code) to collapse the `/tasks` page's four stacked filter blocks (status pills · count band · date-range · search) into one compact filter row, so the task table sits near the top of the page. Same filters, same behaviour.

**Why it parks for you:** three one-liner questions — (1) approve the one-row collapse? (2) keep all four filters or drop any (page-size / big count band)? (3) status as a dropdown or a tightened pill row?

**Heads-up the reviewer flagged:** this feature has no entry in the product brief (currently v1.23). If you approve, the *build* PR will carry a brief amendment at the next free version (per the §9 protocol) — nothing to do now, just noting it.

**Touches at build time:** Session B's lane (`/tasks`), and it builds **after** R6-part-1 (#427) lands (shared page header). No DB. **SQL TO APPLY: no.**

## PR #430 — docs(d53): PLAN — calendar day-popover 8-action grouping (click-reduction)

- Label: `needs-directional-ruling`
- Head SHA: `ddc53e98378ef2561d764610c9b4f22f672dd312`

ORCH-PARK

**What it is:** a PLAN (no code) to group the consignee calendar's day-popover from 8 flat action buttons into 3 always-visible sections (View / Edit delivery / Reschedule) — a scan win with no added click.

**Why it parks for you:** the grouping and labels are a product/UX call only you can make. Three one-liner questions in the plan: (1) approve View / Edit delivery / Reschedule grouping? (2) different grouping/labels? (3) always-visible (recommended) or collapsed groups?

**Touches at build time:** Session C's lane (the calendar drawer). No DB. **SQL TO APPLY: no.** Reviewer: well-formed plan, parked as a Love-only directional question.

## PR #440 — feat(d54): orch-automerge hardening — clearance-merge mode + verdict-comment trigger + dirty-PR loud-park [PARKS]

- Label: `parked-t3`
- Head SHA: `43aea0699291aa45abe2cfad0551daed65a15fee`

(no ORCH-PARK summary comment found — the builder must post one when parking)

## PR #438 — fix(d53): R-B — windowless-resume recovery for auto-paused subscriptions

- Label: `parked-t3`
- Head SHA: `f66f65368606db9f9fbd18e0531a36db9a1f1caf`

ORCH-PARK (Session C)

**Plain English:** This is the code for the stuck-subscription fix. Before: if the system ever auto-pauses a subscription (deliveries failing repeatedly), the Resume button says it worked but nothing changes — the subscription is frozen until someone hand-edits the database. After: clicking Resume actually un-freezes it and writes a proper audit entry. The automatic scheduler is deliberately kept out — only a human click recovers an emergency-halted subscription.

**Before/after, proven on a real database:** the stranding was reproduced live in the new test (paused, no recovery path), then recovery, double-click safety, scheduler exclusion, and the normal operator pause/resume flow were all pinned green.

**Status:** Reviewer APPROVE round 1 (independent body-read at f66f653). CI: lint/typecheck/unit/integration all PASS; the one red check is Vercel's free-tier daily build rate limit — environmental, not this code.

**Needs from Love:** one-line clearance to merge the pair — plan #434 first (or together), then this. No schema, no spend, no brief bump. Reminder from both reviews: the trap can't fire yet (the auto-pause trigger was never wired in), so this lands the safety net first — the right order.

## PR #434 — plan(d53): R-B windowless-resume conformance — auto-pause stranding fix (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `dbbdd40ec8c8d02b1b2f9374085353d2218046c7`

ORCH-PARK (Session C)

**Plain English:** This is the plan (no code yet) for fixing the stuck-subscription trap. Today, if the system ever auto-pauses a subscription because its deliveries keep failing, the Resume button says nothing happened and the subscription stays frozen forever — only a database hand-edit recovers it. The plan: make Resume actually work for that case (a small change in one function), and keep the rule that only a human can un-freeze it, never the scheduler. One honest correction along the way: the trap can't actually fire yet, because the piece that would trigger the auto-pause was never wired in — so we're fixing the lock before anyone installs the door.

**Before/after:** Before — Resume silently lies on an auto-paused subscription. After (once the code PR lands) — Resume recovers it and writes a proper audit trail.

**Status:** Reviewer APPROVE round 1; reviewer also ruled the design choice (Option B, resume-side fix) correct — the deviation from the triage memo's sketch was dispatch-flagged and is resolved. Build proceeds on this clearance per the dispatch; the code PR follows separately.

**Needs from Love:** nothing to decide — one-line clearance to merge this plan record whenever convenient. No schema, no spend, no brief bump.

## PR #435 — feat(d53): Tier-2 H4 — RGB-channel token refactor + /tasks button polish

- Label: `parked-t2`
- Head SHA: `f4fb9b57d915627b55aa3cee7c11cc4f298767dc`

ORCH-PARK

**What it is:** Tier-2 **H4** — the foundational "tint" fix. Brand colours were stored as hex, so every translucent surface in the app (modal dim, alert pills, subtle tints, faded badges) silently rendered as flat/transparent. This rewires the colour tokens so opacity works everywhere at once — **solid colours are unchanged**, the previously-broken see-through effects now render as intended. Plus small `/tasks` button polish (the per-row Cancel/Edit buttons get the same hover-fade + keyboard-focus outline the rest of the app's buttons already use).

**Reviewer: ORCH-VERDICT APPROVE (r1)** — with independent proof: compiled Tailwind on both `main` and this branch and confirmed `bg-navy/40` produces *nothing* today and renders correctly here; all 16 colour values hand-checked against the brand hex (zero colour drift); typecheck clean; 33/33 new guard tests. CI: unit (2076) + integration green.

**One small question for you (the reviewer flagged it):** the `/tasks` buttons now show a keyboard-focus ring. The reviewer read this as a *new* look-and-feel convention. **Factual correction for the record:** focus rings already exist in the app — the `/tasks` "Print labels" button and the Cancel/Save modal buttons already use this exact ring, and the Tier-1 pass (#391) added focus rings to key surfaces. So this **completes the existing recipe** on two buttons that were missed, rather than inventing a convention. Still surfacing it for your one-liner: **keep the focus rings (recommended — it's the established pattern + an accessibility win), or strip them?**

**Touches:** the shared colour-token system + `/tasks` only. No other session's lane. **No DB. SQL TO APPLY: no.** No brief bump (foundational token mechanics; no design-token *values* changed).

**Polish DEFERRED (lane-blocked):** the rest of the audit's polish (DayActionPopover badges, consignee-tab empty states, subscription badge recipes, DLQ button/error wrapper, more focus rings) lives in Session A/C lanes — flagged in the PR body for their hand-off. H4's fix benefits all those translucency cases regardless of who edits the components.

**Note:** Vercel's preview check is red only because of today's free-tier deploy rate-limit (resets ~24h) — unrelated to the code; a parked PR doesn't need it. At clearance, the merge will want it green or an admin land.

**Clears on:** your one-liner (incl. the focus-ring keep/strip call).


# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #497 — fix(d54): /tasks row Cancel/Edit honor the assignment/terminal lock

- Label: `parked-t2`
- Head SHA: `d6252684270b76d48922e197b2b17b539295f007`

ORCH-PARK (Session A, firing item 3)

**Plain English:** the walk found the Tasks LIST still offered Cancel/Edit buttons on deliveries a driver already holds (the server refuses those anyway). Now the row greys both buttons out with the same one-line explanation the calendar shows ('Assigned to a driver — this delivery is locked…'), and final-state rows get a matching message. The status rules now live in ONE shared module used by both the screen and the server, so they can't drift apart.

**RED-first:** three lock cases watched failing, then 5/5 green. No DB, no schema. **SQL TO APPLY: no.** tsc 0 / eslint 0 new / unit 2125.

**Reviewer:** ORCH-VERDICT APPROVE r1 at pinned head `d625268`, CI green.

**Clears on:** your one-line clear.

## PR #496 — feat(d54): churn role gate — consignee:churn, merchant-level only (brief v1.27)

- Label: `parked-t2`
- Head SHA: `30222843d045a109c62c45b6238e6d261659467a`

ORCH-PARK (Session A, firing item 2)

**Plain English:** your standing ruling — churn only from merchant level — was NOT enforced in the code: the Transcorp admin role's all-permissions set could fire the churn hard stop server-side. This PR builds the gate: a new `consignee:churn` permission that merchant roles (Tenant Admin / Ops Manager / CS Agent) hold and Transcorp staff explicitly do NOT — the one exception ever carved out of the admin's everything set. The server refuses before touching data; the Churned option also disappears from the menu for anyone without it.

**RED-first:** three gate tests watched failing before the build. **Brief bumped to v1.27** (next-free, additive-only). No schema. **SQL TO APPLY: no.** tsc 0 / eslint 0 / unit 2127.

**Reviewer:** ORCH-VERDICT APPROVE r1 at pinned head `3022284`, CI green.

**Clears on:** your one-line clear.


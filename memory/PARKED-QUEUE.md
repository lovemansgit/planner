# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #460 — docs(d54): probe memo — SF cancel-on-ASSIGNED BLOCKED + staged script (T1)

- Label: `parked-t3`
- Head SHA: `3def51fb117549f403497b1d319204c45423dbf7`

ORCH-PARK (Session C)

**Plain English:** This holds the staged probe script — the dry-run-verified tool that would try to cancel a delivery already assigned to a driver at the courier, to record whether the courier accepts or refuses. The probe itself could NOT be fired: no sandbox delivery is in the ASSIGNED state, the courier's API gives us no way to put one there, and the closest substitute was correctly blocked as outside the named authorization. The findings memo merged separately (#461, on main).

**Status:** Parked by Love's Day-55 ruling — "#460 (probe script) stays parked — precondition waits on a vendor-side staged assignment via Aqib; not blocking."

**Needs from Love:** nothing now. When Aqib stages an ASSIGNED delivery in the sandbox, re-dispatch the probe and this script fires as-is.


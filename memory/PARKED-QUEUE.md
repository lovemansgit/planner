# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #394 — fix(d53): admin nav 'Calendar' → 'Overview' label (Love's audit ruling)

- Label: `needs-directional-ruling`
- Head SHA: `328ce16c7b2ec252c2101a364ca85ccc5ce0891e`

ORCH-PARK — PR #394, head `328ce16`. Parked `needs-directional-ruling` (Shape-3 §9 clearance-is-verification).

**Love's ruling (verbatim) — this IS the clearance:**
> "Rename the admin nav entry from 'Calendar' to 'Overview' — admin persona only; merchant persona keeps 'Calendar.' Confirmed by Love, 2026-06-11."

**Reviewer r1 (opus): REQUEST_CHANGES — correctly, not a defect.** The reviewer found the code correct, low-risk, and scope-clean (it independently confirmed `/admin/calendar` renders `<h1>Fleet overview</h1>`, that the tenant "Calendar" entry is untouched, that `AdminTopNav` renders `{item.label}` with no hardcoded consumer, and that all 22 nav-config tests pass). It withheld APPROVE for ONE reason: a user-facing label change is a Love-only directional call and the ruling above lives in Love's dispatch, which the reviewer's separate context cannot see (LOVE-TRIGGER, brief drift). Per Standing Order 5 the builder parks rather than revises — done. **No revision was made; nothing is wrong with the change.**

**Verification:** RED-first (2 admin nav-config tests flipped to expect "Overview", watched fail, then the label change → green). Full unit suite **2012/2012**, `tsc` 0, `eslint` 0. **CI GREEN at `328ce16`** (Vercel + lint/typecheck/unit + integration all pass).

**SQL TO APPLY:** none.

**Brief:** the admin nav *label* is an implementation detail not enumerated in the brief — no brief section is contradicted (unlike #391's calendar default, which superseded a BRD section). I did **not** add a brief amendment for a nav-label rename. If you want a one-line brief note recorded, say so and I'll add it; otherwise none is needed.

**Promote:** per Love's dispatch this **rides tonight's routine promote, NOT a pre-UAT one.** It does not gate the UAT now running.

**Clears on:** Love's one-line confirm → merges → rides tonight's routine promote.

## PR #396 — fix(admin): merchant detail AUTH METHOD shows effective method (override-aware)

- Label: `parked-t2`
- Head SHA: `399c0d960339dc62e1320578150748d91dbca374`

(no ORCH-PARK summary comment found — the builder must post one when parking)


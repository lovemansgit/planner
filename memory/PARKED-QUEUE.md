# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #380 — fix(d53): api_key sessions renew via full login — never the unverified OAuth refresh wire [lane code-PR]

- Label: `parked-t3`
- Head SHA: `ad11d6b4a15f46507325d0b5e14c7b50e9a51fd5`

ORCH-PARK

**What this PR does (plain English):** The code half of the SF production-auth lane. Renewal for api_key merchants now happens by a fresh full login (about once a month on the 30-day tokens) instead of by the token-refresh wire that Aqib's docs only verified for the OAuth path — so no unverified wire shape is ever exercised with a production credential. The probe script also gains an observation step that records whether the refresh wire happens to work for api_key tokens, so the open Aqib question closes with evidence whenever you authorize the probe run. Sandbox OAuth behavior is byte-identical.

**What it touches:** one conditional in the token cache, two RED-first tests, the probe script. **No migration.** Unit 2003/2003, tsc + eslint clean; reviewer re-verified independently in a clean checkout.

**Standing park-flags (lane):** (a) real production credential values are entered by you/Aqib via /admin/merchants/[id]/credentials — never through this terminal; (b) the live production probe fires only on your named go.

**Reviewer's verdict (one line):** APPROVE round 1 (opus); no Love-trigger; one non-blocking note — the code comments cite the plan doc that lives in parked #378, so merging #378 first (or together) keeps the pointer resolvable.

**Merge-order note:** clear #378 (plan) before or with this one.

**SQL TO APPLY: no.**

## PR #378 — plan(d53): SF production auth lane (api_key) — renewal strategy + probe extension [T3 plan, parks]

- Label: `parked-t3`
- Head SHA: `bffa2a4038fce9fa288f7264b0af0901130dd62e`

ORCH-PARK

**What this PR does (plain English):** The plan for the SF production-auth lane you opened today. Big finding from grounding: the wiring named in the directive is already merged end-to-end — what's actually left is one design call: api_key sessions should NOT renew over the refresh wire Aqib's docs never verified. The plan proposes renewing by a fresh full login instead (~once a month on 30-day tokens, no unverified wire ever touched in production auth), plus extending the probe script to observe the refresh wire so the open Aqib question closes with evidence whenever you authorize the probe.

**Standing park-flags (both PRs in this lane):** (a) real credential values go in via /admin/merchants/[id]/credentials by you/Aqib only — never through this terminal; (b) the live production probe fires only on your named go. Reviewer reinforced this as a LOVE-TRIGGER: its APPROVE clears the plan text only, not the probe or any secret entry.

**Reviewer's verdict (one line):** APPROVE round 1 (opus), with the cost/external-contract trigger flagged on the probe + provisioning steps (already the plan's own posture).

**SQL TO APPLY: no** — no migration anywhere in this lane.

The companion code-PR (skip-refresh for api_key + probe extension + RED-first tests) parks separately right behind this.

## PR #377 — fix(d53): POD photos render through a same-origin authenticated proxy (UAT-blocking ruling 4)

- Label: `parked-t2`
- Head SHA: `b60953df18c2a0a90c677ea40c3e3cf8f147dcc6`

ORCH-PARK

**What this PR does (plain English):** Closes UAT-blocking ruling 4. POD photos now render through Planner instead of pointing the browser straight at SuiteFleet's expiring S3 links. Grounded first on real rows: the links SF sends are 7-day pre-signed URLs minted a second before the delivery webhook, the old broken thumbnail you saw was the browser refusing S3's response (a server fetch is immune), and links past 7 days are dead at the vendor permanently — for those, the operator now gets a clean "expired at the vendor" response instead of a mystery glyph. **A Planner-only fix WAS achievable** (no Aqib needed); the same-day-delivery UAT scripting fallback is moot. Fresh UAT deliveries will render.

**What it touches:** one new authenticated route (same security posture as the label-PDF proxy — the browser never sees the SF link), one new pure helper module, one service function, one line in the /tasks page. **No migration, no storage, no new spend.** Tests watched fail RED. Unit 2009/2009, tsc + eslint clean.

**Deferred (recorded in the memo, your call later):** durable ingest-time photo capture (needs a migration + a storage decision — would brush the cost trigger), the admin tasks page POD cell, and removing the orphaned CalendarPodCard.

**Reviewer's verdict (one line):** APPROVE round 1 (opus) — security posture matches the labels-route precedent; no Love-trigger fires.

**SQL TO APPLY: no.**

## PR #376 — fix(d53): R8 history metadata strip moves server-side (UAT-blocking ruling 3)

- Label: `parked-t2`
- Head SHA: `ab33ec79a7e0a6b2dd9719c0909261370a65bde5`

ORCH-PARK

**What this PR does (plain English):** Closes UAT-blocking ruling 3 from your Day-53 PM check-in. The task-drawer History section already SHOWED only operator-meaningful fields, but the raw payload behind it (raw SuiteFleet error text, correlation/idempotency plumbing, internal UUIDs) still traveled to the browser and was visible in devtools. The strip now happens on the server, so those fields never leave it; the screen re-filters on the same shared list as belt-and-braces. Nothing an operator sees changes.

**What it touches:** one new shared module (the allow-list + filter), one line-of-substance service change, the drawer swaps its private list for the shared one, one new unit test (watched fail pre-fix). **No migration.** Unit 2002/2002; integration's single failure is the KNOWN pre-existing pagination flake (Session B's followup, passes in isolation with and without this diff).

**Reviewer's verdict (one line):** APPROVE round 1 (opus).

**SQL TO APPLY: no.**


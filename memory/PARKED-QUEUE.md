# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #409 — plan(d53): R12 — resolved failed-pushes page, Path B (/admin/failed-pushes/resolved) — T3 plan-PR

- Label: `needs-directional-ruling`
- Head SHA: `bf46ef8ff6e8ef2283b7f24cd0fdab8cd40bb346`

ORCH-PARK — PR #409, head `bf46ef8`. Parked `needs-directional-ruling` (reviewer APPROVE r1 + LOVE-TRIGGER 2 flagged — a flagged trigger parks regardless of agreement).

**What this PR is:** the R12 plan — a read-only page for resolved failed-push rows at `/admin/failed-pushes/resolved` (Path B from the Day-33 filed memo). No mutations, no new permissions, **no database changes**.

**Reviewer verdict (r1, opus): APPROVE** — plan judged correct and contract-faithful. It flagged one thing for you: the filed memo reserved the Path A/B/C choice to you, and your Path-B ruling lives in the Session C dispatch, which the reviewer's separate context can't see. **Your one line confirming "Path B is my ruling" clears this plan and the code-PR that follows it.**

**Build status:** per your dispatch (PART 3: plan-PR → review → code-PR parks), Session C proceeds to build; the code-PR will park alongside with its own verdict. Nothing merges without you.

**SQL TO APPLY:** none.

## PR #405 — feat(d53): operator adds a second address from the consignee detail page (T3 — parks)

- Label: `needs-directional-ruling`
- Head SHA: `1f0b3db3c70d2ed7dc80ab8224df8ab30c664aa1`

ORCH-PARK (round 2) — PR #405, head `1f0b3db`. Parked `needs-directional-ruling` with the dispute trail in place (max 2 rounds reached; round 2 not APPROVE).

**What happened:** your Day-53 PM dispatch ruled the v1.11 partial retirement, assigned the v1.21 brief bump to this PR, and named #405 cleared-by-firing conditional on reviewer APPROVE. I executed: merged main in, wrote the v1.21 append-only row + header pointer, CI re-verified GREEN at `1f0b3db`, and re-launched the reviewer.

**Reviewer r2 (opus): REQUEST_CHANGES — verification, not defect.** It re-read every file at the pinned head and again found the code clean ("correct cross-tenant existence probe… isPrimary hard-set false… one additive audit entry… CI green"). It withheld APPROVE for one reason: your ruling lives in the dispatch, which its separate context cannot see — from its vantage the v1.21 row is indistinguishable from a self-assigned bump (your three-pair rule 3). Same shape as #394/#398.

**Clears on:** one line from you ON THIS PR (or your next firing naming #405 unconditionally) confirming: "the v1.21 bump on #405 is mine — dispatch-assigned, partial v1.11 retirement confirmed." Then merge via the admin route per your standing authorization, quoting that line. The engineering has now been verified twice; nothing else is open.

**SQL TO APPLY:** none.

## PR #412 — feat(d53): R12 — resolved failed-pushes review page (T3 — parks)

- Label: `parked-t3`
- Head SHA: `4a111afdf1ab28bbbe2f684e2b9243326a332e44`

ORCH-PARK — PR #412, head `7c9f51a`. Parked `parked-t3` with **reviewer APPROVE r2** riding (r1's one mechanical fix — brief renumber to v1.21 — applied and re-verified).

**Before:** resolving failed pushes makes them vanish from the screen — "did my resolve save?" and "who resolved what last month?" need a developer running SQL.
**After:** a read-only **Resolved pushes** page (linked from the work queue) lists every resolved row newest-first: when, by whom (email, or "System"), the notes, and the original failure. No buttons — pure history.

**Reviewer (opus):** r1 found the build "well-built and well-scoped… no product question reserved to you here" with one mechanical renumber; r2 APPROVE after the fix. No Love-triggers fired. CI GREEN at `7c9f51a`.

**Brief:** carries the dispatch-assigned bump as **v1.21** (next-free against current main). #405 (parked, add-address) also carries a v1.21 — whichever of the two you clear SECOND gets renumbered to the then-next-free number before merge; both rows say so.

**SQL TO APPLY:** none — read-only feature, zero database changes.

**Clears on:** your one-line clear (it rides the plan #409's Path-B confirm) → merges via the admin route → rides the next routine promote.

## PR #411 — docs(d53): runbook — firing-as-clearance + builder clearance-merge constraint (supersedes #400)

- Label: `parked-t3`
- Head SHA: `4328ff45b6383b413ff5b6008b16bbad2c0f63c2`

ORCH-PARK — PR #411, head `4328ff4`. `parked-t3`. Reviewer APPROVE r2; CI green at the pinned head (Vercel + lint/typecheck/unit + integration). No SQL. No promote.

This is the rebased #400 (Love-named cleared): the firing-as-clearance amendment + the Ruling-1 constraint + the hardening followup memo (`memory/followup_clearance_merge_into_action.md`), onto post-#398 main.

**Needs Love's manual merge.** The builder cannot self-complete this particular PR — its subject matter is permission-gated on the builder side. One click from Love lands it. (The other three Day-53 clearances — #398/#396/#402 — merged normally.)

## PR #410 — feat(d53): R16 — resume re-activates SF-cancelled deliveries (outbound leg)

- Label: `parked-t3`
- Head SHA: `241606e41990f614ea4b07ae37d7adf845aa19d4`

ORCH-PARK — PR #410, head `241606e41990f614ea4b07ae37d7adf845aa19d4`. Parked `parked-t3` (T3 code; nothing merges without Love). Plan: #408 (APPROVE r1, parked-t3).

**What it does:** on early manual resume, deliveries that were cancelled on SuiteFleet during the pause are now RE-CREATED there through the existing push pipeline (probes proved SF cancel is terminal — un-cancel → 403 — and re-create with the same order number is accepted with a fresh AWB). The restore clears the rows' old SF ids and marks them 'pending'; the push consumer installs the new AWB. New audit event carries the old AWBs for forensics. Bonus fix pinned RED-first: a late vendor webhook for the OLD AWB can no longer knock a restored delivery back to Cancelled. Auto-resume unchanged. **Zero migrations.**

**Verification:** RED-first (5 real-Postgres cases + lifecycle unit updates watched fail) → green; full suites 2012 unit / 488 integration; tsc 0; eslint 0; reviewer r1 caught a brief-table formatting slip (v1.22 row outside the §9 table) — fixed, **APPROVE round 2**. CI green at the head SHA above.

**SQL TO APPLY:** none.

**Brief:** v1.22 row + §3.1.2 event line ride this PR (append-only). ⚠️ The number assumes Session C's v1.21 (#405) lands first — if THIS PR merges first, say so and I renumber to v1.21 before merge (one-line fixup).

**UAT run sheet:** step-H known-limitation note retired (recorded follow-through, day-53 handoff §9).

**Clears on:** Love's one-line confirm + merge order vs #405 → admin-route merge → rides the next routine promote.

## PR #408 — plan(d53): R16 resume re-sync — SF re-activation on early-manual resume (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `a16b01e35d5ddf7d756ed2dc3d7b2344f0c4980b`

ORCH-PARK — PR #408, head `a16b01e35d5ddf7d756ed2dc3d7b2344f0c4980b`. Parked `parked-t3` (T3 plan; nothing merges without Love).

**What it is:** the R16 plan — SF re-activation on early-manual resume, the R2 sibling in reverse. Probes answered both wire questions first (un-cancel REJECTED 403 → re-create path; same-order-number re-create ACCEPTED → new AWB, no dedupe trap). Shape: clear external ids + 'pending' in the restore UPDATE, re-push through the EXISTING push-task pipeline, new audit event with previous-AWB forensics, R2's emit-then-re-throw posture. **Zero migrations** (dispatch expectation verified).

**Reviewer: APPROVE round 1** (body-read at pinned SHA).

**Clears on:** Love's word → merges → the already-built code-PR (next in this lane) parks separately.


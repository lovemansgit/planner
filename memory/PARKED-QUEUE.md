# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #405 — feat(d53): operator adds a second address from the consignee detail page (T3 — parks)

- Label: `needs-directional-ruling`
- Head SHA: `76b500de2bcf901339eb4a7d19459e03c37d37f1`

ORCH-PARK — PR #405, head `76b500d`. Parked `needs-directional-ruling` per the reviewer's r1 verdict (Standing Order 5 — no revision attempt; the blocker is yours, not the code's).

**Before:** onboarding creates one address and promises "add more from the consignee detail page" — but no such button existed, so the proven R4/R5 address-change actions were unreachable for any consignee onboarded through the UI.
**After:** the consignee Overview tab lists all addresses (primary badged) with an **Add address** button, same permission as Edit. New addresses are always non-primary — nothing about existing deliveries changes until an operator explicitly picks the new address in a one-off/forward change or rotation.

**Reviewer verdict (r1, opus): REQUEST_CHANGES — directional only.** Its words: "the engineering is correct and clean… I am NOT approving for one reason, and it is not a code problem." LOVE-TRIGGER 2 (brief drift): this build retires part of the brief's written v1.11 "alternative addresses deferred to Phase 2" line. You already authorized the build (Day-53 EVE ruling: add-address UI builds before production merchants onboard) — the open question is bookkeeping.

**Your one ruling clears both #402 and #405:** confirm the lane is authorized to retire that slice of the v1.11 deferral, and say whether the brief gets an amendment entry recording it — and if so, which session is assigned the version bump (bumps are dispatch-assigned per your three-pair rule 3; Session C filed none).

**Also riding with this park (recorded, no action needed):** the reviewer ruled edit/delete/set-primary/rotation stay Phase-2 (not trivially-same-surface); one cosmetic line of old popover copy ("add a second address from the consignee form first") only renders at zero addresses and was left untouched — reword is an optional follow-up.

**Verification:** RED-first (11 unit + 3 JSX-shape + 4 real-Postgres integration, all watched fail first). CI GREEN at `76b500d` (lint/typecheck/unit + integration on a fresh DB + Vercel build). Local: 2026/2026 unit, tsc 0.

**SQL TO APPLY:** none — no database change anywhere in this lane.

**Clears on:** your one-line ruling above → #402 + #405 merge (admin route, CI re-verified at heads) → rides the next routine promote.

## PR #402 — plan(d53): Session C — operator adds a second address from the consignee detail page (T3 plan-PR)

- Label: `needs-directional-ruling`
- Head SHA: `8974e868c9abe49b01a4f067675079d378f64a83`

ORCH-PARK — PR #402, head `8974e86`. Parked `needs-directional-ruling`.

**What this PR is:** the T3 plan for the add-a-second-address build (Session C's lane) — operator adds a non-primary address from the consignee detail page, so the R4/R5 address overrides become reachable for UI-onboarded consignees. Plan only; no code in this PR.

**Reviewer verdict (r1, opus): APPROVE** — every file:line citation verified at the pinned SHA, including the key claim: **zero database changes** (plain INSERT into the existing addresses table; SQL TO APPLY: none, ever, in this lane). The reviewer also ruled the plan's open question: edit/delete/set-primary stay OUT (Phase 2 stands).

**Why it parks with a question instead of plain parked-t3 — LOVE-TRIGGER 2 (brief bookkeeping):** this build retires part of the brief's written "multi-address UI deferred to Phase 2" line (§3.3.1, v1.11). You already authorized the build itself (Day-53 EVE ruling: add-address UI builds before production merchants onboard). The only open question is yours: **should the brief get a version bump recording that the deferral is partially retired, and if so, which session is assigned it?** Per your three-pair rule 3 (bumps are dispatch-assigned, never self-assigned), Session C filed no brief amendment.

**Build status:** per your dispatch ("BUILD after the plan's reviewer APPROVE"), Session C proceeds to build; the code-PR will park alongside this one with its own reviewer verdict and a plain-English before/after. Nothing merges without you.

**SQL TO APPLY:** none.

## PR #398 — docs(d53): runbook sync — three-pair scaling rules (parks; companion to #397)

- Label: `needs-directional-ruling`
- Head SHA: `59a49b6a5dba6a304bd75fbe745d964cb2e71a06`

ORCH-PARK — PR #398, head `59a49b6`. Parked `needs-directional-ruling` per reviewer Standing Order 5 (no revision attempt).

**What this PR does:** adds one section to the orchestration runbook (`scripts/orchestration/RUNBOOK.md`) recording your three-pair scaling rules: (1) do-not-touch lists in every dispatch; (2) one EOD owner per day — Session A; (3) brief version bumps dispatch-assigned, never self-assigned. Nothing else touched. Companion to the decision memo in #397.

**Reviewer verdict (r1, opus): REQUEST_CHANGES — directional, not a defect.** The reviewer found the change mechanically clean and faithful to the memo, but flagged that a new operating policy for how the build process scales is yours to confirm — your ruling lives in Session C's dispatch, which the reviewer's separate context cannot see (same shape as the #394 precedent). It also noted the memo file referenced here lives in #397, still open — resolved automatically once #397 merges first.

**Clears on:** your one-line confirm that the three-pair scaling ruling quoted in #397 is yours → #397 merges (docs lane) → this merges after it.

**SQL TO APPLY:** none.

## PR #400 — docs(d53): runbook — firing-as-clearance mechanic (autonomy/clearance section)

- Label: `parked-t3`
- Head SHA: `194256ffd3e225d14d7145d9f39c53c23b2df247`

ORCH-PARK — PR #400, head `194256f`. Parked `parked-t3` (off-allowlist by design; reviewer-clean).

**What it is:** folds Love's 2026-06-11 firing-as-clearance amendment into `scripts/orchestration/RUNBOOK.md` (the autonomy/clearance section).

**Reviewer r1 (sonnet): APPROVE.** The text faithfully encodes Love's verbatim amendment; the two-party seam is explicitly preserved; carve-outs (live DB/SQL named authorization, new spend, open reviewer-surfaced decisions) are intact.

**Why it parks (not a defect):** it edits `scripts/orchestration/RUNBOOK.md`, which is OUTSIDE the path-gate allowlist (`memory/**, docs/**, tasks/**, root *.md`). By construction an off-allowlist PR cannot auto-merge — it parks. This is the intended behaviour Love named: "that edit is off the docs allowlist, so it PARKS."

**SQL TO APPLY:** none. **CI:** docs/prose only.

**Clears on:** Love's next dispatch firing that names #400 as cleared-by-firing (the very mechanic this PR documents) → builder merges via gh api (admin route, off-allowlist) and it rides the routine promote. No pre-UAT promote.

## PR #396 — fix(admin): merchant detail AUTH METHOD shows effective method (override-aware)

- Label: `parked-t2`
- Head SHA: `399c0d960339dc62e1320578150748d91dbca374`

ORCH-PARK — PR #396, head `399c0d960339dc62e1320578150748d91dbca374`. Parked `parked-t2` (Shape-3: all code parks).

**What it does:** merchant detail page AUTH METHOD row now shows the EFFECTIVE method (override ?? region default), annotated "(merchant override)" like the credentials page — Love found Demo Bistro's api_key flip invisible there (still read OAUTH off the region's raw method). DTO projects `suitefleet_auth_method_override`; new pure helper mirrors the resolver's selection. Surface sweep: this was the only stale surface (regions pages show their own default correctly; merchants list has no auth column; credentials page already effective-aware).

**Verification:** RED-first — 3 unit + 2 real-Postgres cases watched fail, then green. Full suites 2015 unit / 485 integration, `tsc` 0. Path-gate PARK ✔, reviewer APPROVE round 1 ✔.

**SQL TO APPLY:** none (0030 already applied Day-54 AM).

**Clears on:** Love's one-line confirm + CI green at the head SHA above → merge via the admin route → rides the next routine promote (UI-only; does not gate UAT).


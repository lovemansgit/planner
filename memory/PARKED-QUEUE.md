# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #388 — feat(d54): per-merchant SF auth-method override — sandbox api_key enablement [SQL-TO-APPLY 0030]

- Label: `parked-t3`
- Head SHA: `e32fe1587fe40f647e7734fa78cf93ee2db39953`
- **SQL TO APPLY: yes — supabase/migrations changed. Love applies manually via the Supabase SQL editor.**

ORCH-PARK

**What this PR does (plain English):** The build half of tonight's lane. Any merchant's SuiteFleet authentication method can now be switched between OAuth and Client Credentials from its admin credentials page — per merchant, with the region as the untouched default, exactly your five directives. Switching deliberately wipes the merchant's stored credentials so a half-switched merchant fails loudly in failed-push until you enter the new pair — proven end-to-end on real Postgres (flip via the real service, then resolve → hard error; sibling merchant on the same region keeps resolving OAuth untouched). The credentials page gained the switch panel with a confirm step that states the consequence verbatim. The brief is at v1.19 with the sandbox correction; the probe script and the older records are scrubbed/annotated; your Demo Bistro runbook is staged with the no-plaintext proof shape and a two-action rollback.

**What it touches:** migration 0030 (one nullable column on tenants), resolver, credentials service + new `credentials.method_changed` audit event, the admin credentials page, 6 new real-Postgres tests, brief v1.19, probe header, runbook. Signal: integration 483/483 (fresh DB through 0030), unit 2012/2012, tsc + eslint clean.

**Reviewer's verdict (one line):** APPROVE round 1 (opus).

**SQL TO APPLY: yes — `supabase/migrations/0030_tenants_suitefleet_auth_method_override.sql`, via the Supabase SQL editor on your named authorization, BEFORE this PR's code promotes.** Purely additive (one nullable column + CHECK); zero behavior change for any merchant until a switch is performed.

**Nothing executed live tonight:** no config change, no migration apply, nothing on mpl/UAT data. Demo Bistro's switch + wire proof wait for your clearance sentence (runbook: `memory/runbooks/day-54-demo-bistro-apikey-proof.md`).

## PR #387 — plan(d54): per-merchant SF auth-method switch — sandbox api_key enablement [T3 plan, parks]

- Label: `parked-t3`
- Head SHA: `37bd9e98061fb97afe49dd3da86f7d8830d37bef`

ORCH-PARK

**What this PR does (plain English):** The plan for tonight's lane. Your correction is banked verbatim (SuiteFleet takes both auth methods everywhere — "sandbox is OAuth-only" was our own assumption, now annotated as such on the original decision memo). The shape: one new per-merchant setting that overrides the region's auth method — Demo Bistro can switch to Client Credentials while every other merchant stays on OAuth untouched. Switching a merchant deliberately wipes its stored credentials so a half-switched merchant fails loudly on SuiteFleet pushes (visible in failed-push) instead of silently using the wrong secrets. One reviewer round of substance: the switch event now gets its own minimal audit event per the credentials-event discipline you ratified, instead of riding the merchant-edit event.

**Reviewer's verdict (one line):** REQUEST_CHANGES r1 (audit-event modeling — fixed) → **APPROVE r2** (opus, facts re-verified against main).

**SQL TO APPLY: no** — the migration rides the CODE-PR, which parks separately with the flag.

Code-PR lands next in this same batch.


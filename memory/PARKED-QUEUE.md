# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #447 — feat(d53): R6-part-2 — /tasks AWB opens timeline drawer + R6.3 partial-state banner

- Label: `parked-t3`
- Head SHA: `9ee5bbff2c8ede607ddf998a91bee0cd560ec465`

ORCH-PARK

**What it is:** R6-part-2 — on the `/tasks` page, the **AWB now opens the task timeline** (the same history drawer the consignee calendar uses). Tasks not yet pushed to SuiteFleet (no AWB, shown as "—") also open it, and the drawer shows a banner — *"Task not yet pushed to SuiteFleet — AWB will be assigned once dispatch completes."* — so it reads as an honest empty state instead of looking broken.

**Scope:** the shared timeline-drawer home (the location #421 created for this) + the `/tasks` client. The consignee-calendar caller is **untouched** and behaves exactly as before (the new drawer prop is optional). **No DB. No new SuiteFleet wire. SQL TO APPLY: no.**

**Reviewer:** ORCH-VERDICT APPROVE (r1) — independently confirmed backward-compat (DayActionPopover unchanged → no banner there) and that the banner copy matches your R6.3 ruling verbatim. No Love-trigger. CI: unit (2084) + integration green; Vercel residual is the daily preview-deploy rate-limit only.

**Clears on:** your one-line clear. (This completes R6 — part-1 columns + data layer merged at `7f7d3a3e`; this is the AWB→drawer + partial-state half.)

## PR #445 — fix(d53): R-C — receipt-then-apply tx split; webhook receipt survives apply failure

- Label: `parked-t3`
- Head SHA: `bd4c48e6d67181931a415b9b93d9a2733daaa00e`

ORCH-PARK (Session C)

**Plain English:** This is the code for the lost-webhook-receipt fix. Before: when SuiteFleet notified us about a delivery and our database update failed partway, the permanent record of that notification was erased along with the failed update — SuiteFleet had been told "success", so it never resent, and the event vanished. After: the receipt is saved permanently FIRST, on its own; the update is attempted second and can fail alone. A failed update now leaves the original notification intact as evidence, with the payload preserved.

**Proven on a real database:** the loss was reproduced live on both webhook paths (receipt erased), then the fix pinned: receipt survives the failure, re-delivery is correctly recognized as a duplicate, the normal flow and the unknown-task path are unchanged (52 regression tests green untouched).

**Status:** Reviewer APPROVE round 1 (independent body-read at bd4c48e — "the update logic is moved, not altered"). CI: lint/typecheck/unit/integration PASS; the red Vercel check is the free-tier daily build rate limit, environmental.

**Needs from Love:** one-line clearance to merge the pair — plan #442 first (or together), then this. No schema, no spend, no brief bump.

## PR #442 — plan(d53): R-C receipt-then-apply tx split — webhook receipt survives apply failure (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `30c682c3e5d9001f8f6090e2f8de794aa37f0f7d`

ORCH-PARK (Session C)

**Plain English:** This is the plan (no code yet) for the lost-webhook-receipt fix. Today, when SuiteFleet notifies us about a delivery and our database update fails halfway, the permanent record of that notification is erased along with the failed update — and since SuiteFleet was told "success", it never resends. The event vanishes; the only trace is an error alert. The plan: save the receipt first, permanently, then attempt the update — if the update fails, the receipt survives as evidence with the original payload intact.

**Status:** Reviewer APPROVE round 1 — every claim independently re-verified against the code, fix shape follows the merged triage memo verbatim, trade-off ruled honest (nothing real is lost; SuiteFleet never resends after a success response anyway). Build proceeds on this clearance per the dispatch.

**Needs from Love:** nothing to decide — one-line clearance to merge this plan record whenever convenient. No schema, no spend, no brief bump.

## PR #440 — feat(d54): orch-automerge hardening — clearance-merge mode + verdict-comment trigger + dirty-PR loud-park [PARKS]

- Label: `parked-t3`
- Head SHA: `43aea0699291aa45abe2cfad0551daed65a15fee`

(no ORCH-PARK summary comment found — the builder must post one when parking)

## PR #438 — fix(d53): R-B — windowless-resume recovery for auto-paused subscriptions

- Label: `parked-t3`
- Head SHA: `f66f65368606db9f9fbd18e0531a36db9a1f1caf`

ORCH-PARK (Session C)

**Plain English:** This is the code for the stuck-subscription fix. Before: if the system ever auto-pauses a subscription (deliveries failing repeatedly), the Resume button says it worked but nothing changes — the subscription is frozen until someone hand-edits the database. After: clicking Resume actually un-freezes it and writes a proper audit entry. The automatic scheduler is deliberately kept out — only a human click recovers an emergency-halted subscription.

**Before/after, proven on a real database:** the stranding was reproduced live in the new test (paused, no recovery path), then recovery, double-click safety, scheduler exclusion, and the normal operator pause/resume flow were all pinned green.

**Status:** Reviewer APPROVE round 1 (independent body-read at f66f653). CI: lint/typecheck/unit/integration all PASS; the one red check is Vercel's free-tier daily build rate limit — environmental, not this code.

**Needs from Love:** one-line clearance to merge the pair — plan #434 first (or together), then this. No schema, no spend, no brief bump. Reminder from both reviews: the trap can't fire yet (the auto-pause trigger was never wired in), so this lands the safety net first — the right order.

## PR #434 — plan(d53): R-B windowless-resume conformance — auto-pause stranding fix (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `dbbdd40ec8c8d02b1b2f9374085353d2218046c7`

ORCH-PARK (Session C)

**Plain English:** This is the plan (no code yet) for fixing the stuck-subscription trap. Today, if the system ever auto-pauses a subscription because its deliveries keep failing, the Resume button says nothing happened and the subscription stays frozen forever — only a database hand-edit recovers it. The plan: make Resume actually work for that case (a small change in one function), and keep the rule that only a human can un-freeze it, never the scheduler. One honest correction along the way: the trap can't actually fire yet, because the piece that would trigger the auto-pause was never wired in — so we're fixing the lock before anyone installs the door.

**Before/after:** Before — Resume silently lies on an auto-paused subscription. After (once the code PR lands) — Resume recovers it and writes a proper audit trail.

**Status:** Reviewer APPROVE round 1; reviewer also ruled the design choice (Option B, resume-side fix) correct — the deviation from the triage memo's sketch was dispatch-flagged and is resolved. Build proceeds on this clearance per the dispatch; the code PR follows separately.

**Needs from Love:** nothing to decide — one-line clearance to merge this plan record whenever convenient. No schema, no spend, no brief bump.

## PR #448 — feat(d53): #430 — day-popover action grouping (always-visible sections)

- Label: `parked-t2`
- Head SHA: `a77496be171b0792f22317e73e05450d8300e39d`

ORCH-PARK

**What it is:** the build of your approved click-reduction plan **#430** — the calendar day-popover's flat list of action cards now groups under three always-visible headers (**Edit delivery** / **Reschedule** / **View**). The operator scans three groups instead of eight equal-weight cards. **Nothing is collapsed and no action gains a click** — each is still one click; empty groups simply don't render.

**Under the hood:** the action list + the new grouping moved into a small pure `day-actions.ts` so the grouping is unit-tested; the popover behaves exactly as before (a move, not a rewrite). The consignee calendar's other components are unaffected.

**Lane:** calendar UI (dispatch-assigned to me tonight), not a push/webhook module. **No DB. SQL TO APPLY: no.** No brief bump.

**Reviewer:** ORCH-VERDICT APPROVE (r1). CI: unit (2087) + integration green; Vercel residual is the daily preview-deploy rate-limit only.

**Run-sheet:** no step gains a click (always-expanded groups) — flows C/E/F/G/H/I/J keep their click counts; only the on-screen grouping/labels change. **Clears on:** your one-line clear.

## PR #446 — feat(d53): #431 — /tasks filter row collapse (+ brief v1.24)

- Label: `parked-t2`
- Head SHA: `c86c3d987f0edccfa5e51abb7272edb11576449e`

ORCH-PARK

**What it is:** the build of your approved click-reduction plan **#431** — the `/tasks` page's three filter controls (search, status pills, date range), previously three stacked full-width blocks, now sit on **one compact row** so the task table starts higher up. Nothing removed; same filtering, same shareable URLs.

**Brief:** carries the dispatch-assigned **§9 v1.24** amendment (additive-only — new row + version pointers; no existing row touched). v1.24 is next-free against current main; re-confirm/renumber at merge-prep if a peer bump lands first.

**Scope:** `/tasks` page layout + the brief only. Shared SearchBar/DateRangeFilter (8 pages) untouched. Hero-count treatment unchanged (deferred to `<HeroCount>`). No DB. **SQL TO APPLY: no.**

**Reviewer:** ORCH-VERDICT APPROVE (r1). CI: unit (2080) + integration green; Vercel residual is the daily preview-deploy rate-limit only (resets ~24h) — irrelevant to a parked PR; at clearance the merge wants it green or an admin land.

**Clears on:** your one-line clear.


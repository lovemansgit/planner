# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #423 — feat(d53): durable POD photo capture + H3 expired-at-vendor placeholder [SQL-TO-APPLY 0031]

- Label: `parked-t3`
- Head SHA: `46993931bd4802a8652f4ae8b4b0c25c84d93e11`
- **SQL TO APPLY: yes — supabase/migrations changed. Love applies manually via the Supabase SQL editor.**

ORCH-PARK — PR #423, head `46993931bd4802a8652f4ae8b4b0c25c84d93e11`. Parked `parked-t3` (T3 code; nothing merges without Love). Plan: #413 (MERGED `bb9e814` on your Ruling A).

**What it does:** every delivery photo gets copied out of SuiteFleet within minutes of the DELIVERED webhook into Planner's own private storage (free tier, $0 per your ruling) — so photos keep working forever instead of dying at SF's 7-day link expiry. The photo viewer prefers the durable copy automatically. Old expired photos now show a **styled "Photo expired at the delivery vendor" image** instead of the browser's broken-image icon (H3). If storage usage nears the free 1 GB cap you get a loud alert naming the Pro-upgrade GTM memo — photos are never silently dropped.

**Reviewer: APPROVE round 1** (full body-read at the pinned head).

**⚠️ SQL TO APPLY: yes — migration 0031** (one new column on tasks + one CHECK extension; additive, inert until the code runs). **Waits for your NAMED authorization** — never firing-cleared. Apply before/at the promote that carries this PR.

**Fences honored:** src/modules/tasks/** and src/app/(app)/tasks/** untouched (new pod-capture module owns its own SQL); no addresses/consignee files.

**Run sheet:** step-D POD lines updated in this PR (the broken-image observable is superseded by the styled state — your Tier-2 H3 ruling).

**Brief:** no bump was dispatch-assigned to this lane — say the word at clearance if you want a §9 row.

**Clears on:** your one-line confirm + the named 0031 authorization → merge → rides the next routine promote.

## PR #411 — docs(d53): runbook — firing-as-clearance + builder clearance-merge constraint (supersedes #400)

- Label: `parked-t3`
- Head SHA: `4328ff45b6383b413ff5b6008b16bbad2c0f63c2`

ORCH-PARK — PR #411, head `4328ff4`. `parked-t3`. Reviewer APPROVE r2; CI green at the pinned head (Vercel + lint/typecheck/unit + integration). No SQL. No promote.

This is the rebased #400 (Love-named cleared): the firing-as-clearance amendment + the Ruling-1 constraint + the hardening followup memo (`memory/followup_clearance_merge_into_action.md`), onto post-#398 main.

**Needs Love's manual merge.** The builder cannot self-complete this particular PR — its subject matter is permission-gated on the builder side. One click from Love lands it. (The other three Day-53 clearances — #398/#396/#402 — merged normally.)

## PR #410 — feat(d53): R16 — resume re-activates SF-cancelled deliveries (outbound leg)

- Label: `parked-t3`
- Head SHA: `5208d3fd43b05bfe059843ae3569eb68cf4ee8ab`

ORCH-PARK — PR #410, head `241606e41990f614ea4b07ae37d7adf845aa19d4`. Parked `parked-t3` (T3 code; nothing merges without Love). Plan: #408 (APPROVE r1, parked-t3).

**What it does:** on early manual resume, deliveries that were cancelled on SuiteFleet during the pause are now RE-CREATED there through the existing push pipeline (probes proved SF cancel is terminal — un-cancel → 403 — and re-create with the same order number is accepted with a fresh AWB). The restore clears the rows' old SF ids and marks them 'pending'; the push consumer installs the new AWB. New audit event carries the old AWBs for forensics. Bonus fix pinned RED-first: a late vendor webhook for the OLD AWB can no longer knock a restored delivery back to Cancelled. Auto-resume unchanged. **Zero migrations.**

**Verification:** RED-first (5 real-Postgres cases + lifecycle unit updates watched fail) → green; full suites 2012 unit / 488 integration; tsc 0; eslint 0; reviewer r1 caught a brief-table formatting slip (v1.22 row outside the §9 table) — fixed, **APPROVE round 2**. CI green at the head SHA above.

**SQL TO APPLY:** none.

**Brief:** v1.22 row + §3.1.2 event line ride this PR (append-only). ⚠️ The number assumes Session C's v1.21 (#405) lands first — if THIS PR merges first, say so and I renumber to v1.21 before merge (one-line fixup).

**UAT run sheet:** step-H known-limitation note retired (recorded follow-through, day-53 handoff §9).

**Clears on:** Love's one-line confirm + merge order vs #405 → admin-route merge → rides the next routine promote.

## PR #408 — plan(d53): R16 resume re-sync — SF re-activation on early-manual resume (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `a16b01e35d5ddf7d756ed2dc3d7b2344f0c4980b`

ORCH-PARK — PR #408, head `a16b01e35d5ddf7d756ed2dc3d7b2344f0c4980b`. Parked `parked-t3` (T3 plan; nothing merges without Love).

**What it is:** the R16 plan — SF re-activation on early-manual resume, the R2 sibling in reverse. Probes answered both wire questions first (un-cancel REJECTED 403 → re-create path; same-order-number re-create ACCEPTED → new AWB, no dedupe trap). Shape: clear external ids + 'pending' in the restore UPDATE, re-push through the EXISTING push-task pipeline, new audit event with previous-AWB forensics, R2's emit-then-re-throw posture. **Zero migrations** (dispatch expectation verified).

**Reviewer: APPROVE round 1** (body-read at pinned SHA).

**Clears on:** Love's word → merges → the already-built code-PR (next in this lane) parks separately.

## PR #421 — refactor(d53): relocate TaskTimelineDrawer + timeline actions to shared location (parks; unblocks R6-part-2)

- Label: `parked-t2`
- Head SHA: `624942e5ab7e44f08124bd59973009f49648bed3`

ORCH-PARK — PR #421, head `624942e`. Parked `parked-t2` with **reviewer APPROVE r1** riding.

**Relocation only, no behavior delta.** The task-timeline drawer + its two server actions moved from Session C's consignee route dir to the shared `src/components/task-timeline/` home so Session B's R6-part-2 (/tasks AWB→drawer entry point) can import them. The consignee detail page renders identically — same components, same actions, same permission gates.

**Before/after import map** (full table in the PR body): `TaskTimelineDrawer.tsx` consignees/[id]/_components → components/task-timeline (git mv); `getTaskTimelineAction` + `getTaskHistoryAction` (+ result types) _calendar-actions.ts → components/task-timeline/actions.ts, verbatim — the history action is a required ride-along (the drawer imports it; leaving it would keep the coupling the move exists to break). DayActionPopover's import path updated; breadcrumb left in _calendar-actions.

**Verification:** RED-first location-pin spec; 2017/2017 unit, tsc 0, lint delta 0; CI GREEN.

**SQL TO APPLY:** none.

**Clears on:** your one-line clear → merges → unblocks Session B's R6-part-2.


# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #480 — feat(d54): R-E — churn hard-stop cascade (brief v1.26)

- Label: `parked-t3`
- Head SHA: `f7d3d5817b7c22d65ffc801eac8ffbb3ff86d2bc`

ORCH-PARK (Session C)

**Plain English:** This is the code for your churn ruling. Marking a customer CHURNED is now a true hard stop: every subscription ends, deliveries never sent to the courier are cancelled on the spot, and deliveries already at the courier — including in a driver's hands — get an immediate recall request. A delivery only shows 'cancelled' once the courier confirms; if the courier refuses, the calendar says so honestly: 'Vendor refused recall — final delivery'. The churn dialog carries the mandatory warning and the confirm button reads 'Churn — stop everything'.

**The reviewer's round-1 catch, fixed:** the nightly background job could have quietly re-created a cancelled delivery at the courier (a hole that also existed for paused/skipped deliveries) — closed at three layers and pinned by tests on a real database.

**Status:** Reviewer APPROVE round 2. CI tests green (Vercel red = daily free-tier limit). Brief v1.26 row + §3.1.4 amendment ride along.

**Needs from Love — clearance ORDER matters today:** #461 (probe memo) → #476 (this plan) → #464 → #472 (R-A pair, takes v1.25) → #475 (R-D) → this (#480, takes v1.26; whichever of #472/#480 lands second gets the one-line §9 renumber check at merge-prep — additive-only diff verified then).

## PR #477 — docs(d54): clearance-execution ruling — retire manual-Love steps (runbook + memo + queue-script line)

- Label: `parked-t3`
- Head SHA: `28c0e41625782f8e61d6ce38b9d15148b0959766`

ORCH-PARK — parked by orch-automerge: a server-side gate check failed (see the Action run for which step). This PR does not auto-merge.

## PR #476 — plan(d54): R-E — churn hard-stop cascade (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `bf4470cd1d0718016b903ef5de4b659dc888ac57`

ORCH-PARK (Session C)

**Plain English:** This is the plan for your churn ruling. Marking a customer CHURNED becomes a true hard stop: all their subscriptions end, every pending delivery is cancelled at the vendor immediately — including recall attempts on deliveries already in a driver's hands — behind a mandatory warning popup. Honesty rule: a delivery only shows 'cancelled' once the vendor confirms; if the vendor refuses a recall, the delivery keeps its true status and gets a visible 'vendor refused recall — final delivery' flag. Verified along the way (no drift): ending a single subscription already behaves the softer way you ruled — pending deliveries continue.

**Status:** Reviewer APPROVE round 2 (round 1 was one provenance line — the probe memo isn't on main yet; it's armed on #461, blocked only by the Vercel daily limit). **Merge order when you clear: #461 → this plan (#476) → the code PR.**

**Needs from Love:** nothing to decide — the plan implements your verbatim ruling (quoted in the plan header; the reviewer asks you to glance at it to confirm your own words). Build proceeds on this clearance.

## PR #475 — fix(d54): R-D — DLQ visibility for reconcile-recovered local-write failures (T2)

- Label: `parked-t3`
- Head SHA: `df5b50453850d6275557b48ab6f4f5559f42fd2b`

ORCH-PARK (Session C)

**Plain English:** When a delivery push hiccups in a very specific way (the vendor already has the task, we recovered its id, but our own database write then failed), the operations queue used to show a misleading entry and the recovered id was buried in error monitoring. Now the queue entry says exactly what's true — 'the vendor side is FINE, only our local update failed, here's the vendor id' — so ops can fix it in seconds instead of re-diagnosing. Retry behavior deliberately unchanged; no duplicate-delivery risk exists on this path (triage-confirmed).

Also files (not builds) the sibling follow-up the dispatch ordered: the same blind spot exists on the plain success path, where it's riskier — anchored in `memory/followup_step5_mark_pushed_failure_no_dlq.md` for a future one-line dispatch.

**Status:** Reviewer APPROVE round 1. CI: both test gates PASS; Vercel red is the daily free-tier build limit (environmental).

**Needs from Love:** one-line clearance to merge. No schema, no spend, no brief bump.

## PR #472 — fix(d54): R-A — cutoff is creation-only; editability locks at assignment (brief v1.25)

- Label: `parked-t3`
- Head SHA: `3c58e32b5c877ad0a5d2398d961dc9f05baa8db3`

ORCH-PARK (Session C)

**Plain English:** This is the code for your delivery-lock ruling. The 18:00 clock now only gates CREATING orders (and skips/appends, which create or cancel orders by date). An existing delivery is freely editable and cancellable at ANY hour — right up until a driver takes it, at which point it locks completely. Driver notes still go through after assignment (a note is a message, not an edit — reviewer-ruled on the plan, override with one line if you disagree). Pausing a subscription no longer cancels deliveries already in driver hands — they complete, and the record says so honestly.

**Proven:** 18 behavior pins watched failing before the build — including proofs the OLD clock is really gone (late-night edits/cancels/pauses now succeed on unassigned deliveries), not just that the new lock exists. Real-database tests: a pause over a mixed window freezes the assigned delivery and cancels the unassigned one; skipping a driver-held delivery is refused outright. CI caught the old rule's own pin ("ASSIGNED cancel succeeds") — flipped to the ruled behavior.

**Brief:** §3.1.8 rewritten + v1.25 row (v1.16 row untouched, append-only). Re-verify next-free at merge-prep.

**Status:** Reviewer APPROVE round 1. CI fully green.

**Needs from Love:** one-line clearance to merge the pair — plan #464 first (or together), then this.

## PR #470 — fix(d54): R16 open-ended resume — restore + re-activation run for every early manual resume

- Label: `parked-t3`
- Head SHA: `47acac6362b58a9e891c4cbdcdf51034861b58d1`

(no ORCH-PARK summary comment found — the builder must post one when parking)

## PR #469 — plan(d54): R16 open-ended resume fix — guard split (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `2f2b08134b8c0a00a34fbd666eb1aa70eb33e014`

(no ORCH-PARK summary comment found — the builder must post one when parking)

## PR #464 — plan(d54): R-A — cutoff = creation-only; editability = assignment gate (T3 plan-PR)

- Label: `parked-t3`
- Head SHA: `fbfdf7bc71d6c5a42a5ca6e699d4d47708f34fcd`

ORCH-PARK (Session C)

**Plain English:** This is the plan for your delivery-lock ruling. Today every edit and cancel is blocked by the 18:00 clock; after this builds, the clock only applies to CREATING orders (and skips/appends, which create/cancel orders by date), while an existing delivery is locked the moment a driver takes it — and freely editable any hour before that. The plan maps all ten places the old clock lives and exactly what each becomes.

**Two in-round rulings recorded for your eyes (override with one line at clearance if you disagree):**
1. **Driver notes stay possible after assignment** — the reviewer ruled a note to the driver is a message, not an edit; locking it at assignment would kill the feature exactly when it matters.
2. **Address changes follow the assignment rule, not the clock** — fixing an address on an unassigned delivery is allowed at any hour; once a driver has it, locked.

**Status:** Reviewer APPROVE round 2 (round 1 caught a real gap — address overrides were accidentally staying clock-gated — fixed). Build proceeds on this clearance; the code PR follows with the brief v1.25 amendment.

**Needs from Love:** nothing now — confirm the verbatim ruling text quoted in the plan when you clear the pair, per the reviewer's note.

## PR #460 — docs(d54): probe memo — SF cancel-on-ASSIGNED BLOCKED + staged script (T1)

- Label: `parked-t3`
- Head SHA: `3def51fb117549f403497b1d319204c45423dbf7`

(no ORCH-PARK summary comment found — the builder must post one when parking)

## PR #452 — fix(d54): orch-automerge wait-not-park — missing verdict WAITS; only rejection at head parks

- Label: `parked-t3`
- Head SHA: `6e7b12f1ef1bd7acf8c77cbd8635de2158989bf0`

ORCH-PARK — PR #452, head `6e7b12f`. Parked `parked-t3` (workflow change; the permission layer correctly ruled the builder cannot self-clear it — Love cleared #440, not this follow-up).

**What it is:** the wait-not-park conformance fix to #440. As merged, #440's verdict-comment trigger is dead on arrival: the LABEL run's verdict gate exits 1 when no verdict exists yet, the park handler strips `automerge-t1`, and the later verdict comment resolves mode=none. The #429 stall #440 was cleared to kill is still alive (the morning trigger proof on the queue-regen PR demonstrates it live).

**The fix:** missing/older-head verdict → neutral WAIT (label kept; the verdict comment re-triggers and arms). Only an explicit REQUEST_CHANGES at the current head parks. **Merge conditions unchanged** — arming still requires APPROVE at head. Reviewer APPROVE r1 at the pinned head; CI green (one unrelated flake rerun, the #453 tiebreaker class).

**Clears on:** your one sentence naming #452 cleared → it rides the Action/clearance mechanic → the verdict-comment trigger is then provable end-to-end.

## PR #481 — docs(d54): love-cleared allow-rule — reconciliation audit + shared-identity resolved-by-ruling

- Label: `parked-t2`
- Head SHA: `7f1f884cc92744c214af62daed21ce8760031120`

ORCH-PARK (parked-t2) — PR #481

**What it does:** records Love's 2026-06-12 ruling that the `love-cleared` allow-rule is agent-appliable, files the standing reconciliation audit in the orchestration runbook, and closes the shared-identity open question as resolved-by-ruling (revisit post-UAT). Documentation only.

**What it touches:** `scripts/orchestration/RUNBOOK.md` + three `memory/` docs. No code, no settings, no workflows, no data path.

**Reviewer verdict:** APPROVE at `7f1f884`, with LOVE-TRIGGER(1) flagged-not-resolved — the allow-rule (already landed at `14709c0`) is global to every local session; the reconciliation audit is detective, not preventive. You already ruled with that caveat understood, so it is surfaced per standing order, not reopened.

**SQL TO APPLY:** no.

**Why parked:** edits the orchestration runbook → off the docs auto-merge allowlist. Clear by sentence, or apply `love-cleared`.


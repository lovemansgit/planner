# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #454 — plan(d54): F-1 — apply migration 0025 to shared dev DB (SQL-TO-APPLY, plan only)

- Label: `parked-t3`
- Head SHA: `357ae020ab6f15cf6df81be2bbb00767d6bac206`

ORCH-PARK (Session C)

**Plain English:** This is the plan (nothing executed) for catching the shared development database up with one migration it missed (0025 — it widens a rule to allow the value 'reschedule'). Our test suite has had one standing red locally because of this gap; the production-style CI database is fine. The plan quotes the migration's own two statements verbatim, runs them in a single transaction on the dev/sandbox database only, and verifies with a read-only query plus the standing red test going green.

**Status:** Reviewer APPROVE round 1. **Parks SQL-TO-APPLY** — this touches the live dev database that carries your UAT demo data, so it waits for your NAMED authorization (e.g. "apply 0025 to the dev DB"). Per your dispatch, nothing is built or run past this park.

**Needs from Love:** the named one-liner above when ready. No production DB, no new SQL authored, no spend.

## PR #453 — fix(d54): F-2 — id DESC tiebreaker on subscription list ORDER BYs (pagination determinism)

- Label: `parked-t3`
- Head SHA: `e3f9260db10c3e7b63b9de35f1583471e3ca28c4`

ORCH-PARK (Session C)

**Plain English:** This fixes the coin-flip bug in the admin subscriptions list. When several subscriptions are created in the same batch they share an identical creation timestamp, and the database was allowed to order ties however it liked — so page 1 and page 2 could legally show the same row (the flaky test we kept seeing). The fix adds a deterministic tiebreaker to the sort on that list and three sibling lists, so page order is now stable everywhere.

**Status:** Reviewer APPROVE round 1. CI fully green (incl. Vercel — the daily limit reset). RED-first: the four sort-shape tests were watched failing before the one-line-per-site fix.

**Needs from Love:** one-line clearance to merge. No schema, no spend, no brief bump.

## PR #452 — fix(d54): orch-automerge wait-not-park — missing verdict WAITS; only rejection at head parks

- Label: `parked-t3`
- Head SHA: `6e7b12f1ef1bd7acf8c77cbd8635de2158989bf0`

ORCH-PARK — PR #452, head `6e7b12f`. Parked `parked-t3` (workflow change; the permission layer correctly ruled the builder cannot self-clear it — Love cleared #440, not this follow-up).

**What it is:** the wait-not-park conformance fix to #440. As merged, #440's verdict-comment trigger is dead on arrival: the LABEL run's verdict gate exits 1 when no verdict exists yet, the park handler strips `automerge-t1`, and the later verdict comment resolves mode=none. The #429 stall #440 was cleared to kill is still alive (the morning trigger proof on the queue-regen PR demonstrates it live).

**The fix:** missing/older-head verdict → neutral WAIT (label kept; the verdict comment re-triggers and arms). Only an explicit REQUEST_CHANGES at the current head parks. **Merge conditions unchanged** — arming still requires APPROVE at head. Reviewer APPROVE r1 at the pinned head; CI green (one unrelated flake rerun, the #453 tiebreaker class).

**Clears on:** your one sentence naming #452 cleared → it rides the Action/clearance mechanic → the verdict-comment trigger is then provable end-to-end.

## PR #455 — feat(d53): Tier-2 #5 — shared <HeroCount> + recorded 4b fix

- Label: `parked-t2`
- Head SHA: `e5eb1f3d582794fd5b2d291c91a004f6fcae66af`

ORCH-PARK

**What it is:** Tier-2 #5 first piece — a shared **`<HeroCount>`** for the big-count strip at the top of list pages, so that "big number" treatment lives in one place instead of being re-typed per page (and can't drift). Applied with **no visual change** on `/tasks` and `/consignees` (they already used the canonical look). Also folds the recorded big-number fix: the **subscriptions** page's oversized count is brought down to the standard size — **exactly that one recorded line**, nothing else in that lane.

**Scope:** the new shared component + `/tasks` + `/consignees` (my lanes) + one line on subscriptions. The admin list pages + subscriptions' structural unification are left for their lanes to adopt `<HeroCount>` next. **No DB. SQL TO APPLY: no.** No brief bump.

**Reviewer:** ORCH-VERDICT APPROVE (r1) — confirmed the extraction is visual-identical and the subscriptions change is exactly one line. CI: unit (2088) + integration green.

**Clears on:** your one-line clear.


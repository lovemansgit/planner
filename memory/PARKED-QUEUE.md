# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #488 — fix(d54): orch-automerge — disarm auto-merge on synchronize (post-arm push gap)

- Label: `parked-t3`
- Head SHA: `5f528b0fb4e39f59d6dad65add27197f5e5d2abf`

ORCH-PARK (Session A, assembly dispatch item 5)

**Plain English:** this closes the auto-merge loophole we used (deliberately, as the dispatched remedy) twice today: once the bot arms a merge, a later push to that PR would still merge at the new, unreviewed code. After this fix, any push to an armed PR switches the auto-merge off and says so in a comment; the PR then simply waits for a fresh reviewer verdict at the new code, which re-arms it through all the usual server-side checks. Nothing about WHO can merge changes.

**Scope:** one file, `.github/workflows/orch-automerge.yml` (+41/−1). No app code, no DB. **SQL TO APPLY: no.**

**Reviewer:** ORCH-VERDICT APPROVE r1 at pinned head `5f528b0`. Parks per dispatch (build + park — workflow change is off-allowlist by design).

**Clears on:** your one-line clear.


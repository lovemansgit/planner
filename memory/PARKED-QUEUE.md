# PARKED QUEUE — items awaiting Love

_Generated by `scripts/orchestration/parked-queue.sh`. Do not hand-edit;_
_the park labels on open PRs are the source of truth._

## PR #509 — feat(d54): bag-tracking P3 — Asset Log, manual refresh, UAT addendum (parks; completes the stack)

- Label: `needs-directional-ruling`
- Head SHA: `bc38c705899fb71eb1701eddce83260a2ad63ac0`

ORCH-PARK (Session B, bag-tracking P3 — completes the stack)

**Plain English:** stage 3 finishes the build. The Asset Log (every scan, newest first, each line marked 'recorded in Planner' until SuiteFleet starts sending real scanner times — that's the honest version of the timestamp, per your ruling), the 'Refresh now' button on all three report pages, and your UAT walk script. The Allocated Asset number on the Transcorp report now opens the log, exactly as the business request asked.

**ONE QUESTION FOR YOU (reviewer cost trigger, both rounds):** 'Refresh now' is a human-triggered call into SuiteFleet's metered API. Each click is bounded (at most ~20 batched calls), but nothing stops repeated clicking — two impatient operators could fire sweeps back-to-back. **Recommendation: accept for the pilot as-is** (tiny operator count, the per-click bound holds, and the SF throttle in the adapter still applies) **and add a 5-minute server-side cooldown as a fast-follow if pilot usage shows hammering.** Alternative: I build the cooldown now (one small commit, ~half a day with tests). Your one-line call.

**Honest proof line (reviewer process note):** the lint/typecheck/test CI gate only fires on PRs targeting main — this stack targets its own base branches, so the 2159-unit / 15-integration figures are builder-local runs (reviewer independently re-verified tsc = 0 and confirmed zero new lint findings; the 35 eslint hits on full-repo are pre-existing main debt in untouched files). CI runs in full when the stack collapses onto main after your preview sign-off.

**SQL TO APPLY: no** (this PR; the three migrations park in #507).

**Reviewer:** ORCH-VERDICT APPROVE r1 @ `c81dbdd` + APPROVE r2 @ `bc38c70` (r2 covers the dangling-memo fix the r1 noted; the cost trigger stands in both — it's yours, not ours).

**Clears on:** your cooldown call above + the stack-wide preview sign-off sentence (posture 7a). The preview deployment is already READY: https://planner-git-feat-d54-bagtrack-p3-l-a0ce05-lovemansgits-projects.vercel.app — the walk itself still needs the four gates listed in memory/uat_addendum_bag_tracking.md (named SQL go for 0032-0034, your walk-merchant flag sentence, staged scan data, optional schedule registration).

## PR #502 — plan(d54): bag tracking reports — Asset Tracking + Inventory (T3 plan-PR, PLAN ONLY)

- Label: `needs-directional-ruling`
- Head SHA: `0f6163981727964e5e1908c98140c97e9eff0d12`

ORCH-PARK (Session B, bag-tracking plan lane)

**Plain English:** you pulled the post-MVP bag-tracking item forward and asked for a PLAN ONLY. This PR is that plan. The probe answered the gating question: the asset-tracking endpoint answers to the credentials we already hold (no new auth needed from Aqib), and batch lookups are accepted — but the sandbox still has zero real asset records, so the inner data shape is still doc-based. The plan proposes: keep the live cache we built on Day 6 as the current-state store, add a never-overwritten scan-log table for the Asset Log, and build the two reports (admin Asset Tracking + merchant/admin Inventory) on top — about 5–6 working days in 3 stages. One real schema finding: your screenshots show a 'Sorted' column that our database constraint doesn't allow yet, so a small schema change is named in the plan.

**Build gates:** three migrations are NAMED but not created — they will park for your named authorization at build time. **SQL TO APPLY: no** (this PR is docs + a read-only probe script only).

**Reviewer:** ORCH-VERDICT APPROVE r1 at pinned head `34afc6e`.

**Clears on:** your answers to the 10 numbered questions in the plan (each has one recommendation — 'all recommendations' is a valid one-line answer) + your one-line clear to start the build.

## PR #512 — docs(d54): parked-queue regen — scope-literal ruling filing parked (#511)

- Label: `parked-t3`
- Head SHA: `815a8b3d737580c254b6ea443a7c1105abc9a502`

ORCH-PARK — parked by orch-automerge: a server-side gate check failed (see the Action run for which step). This PR does not auto-merge.

## PR #513 — fix(d54): bag-tracking P4 — walk findings F1 (all-merchants Inventory) + F2 (admin nav overflow)

- Label: `parked-t2`
- Head SHA: `cd8f2fdbdef40a87ccf5c97bfd5dd7874f595e1c`

ORCH-PARK

PR: #513 (bag-tracking P4 — walk findings F1 + F2)
SHA: cd8f2fdbdef40a87ccf5c97bfd5dd7874f595e1c
PATH-GATE: PARK (feature code outside the auto-merge allowlist)
REVIEWER: APPROVE round 1 at the pinned head (no Love-triggers; cost fan-out deliberately avoided)
PROOFS (builder-local): tsc 0 · eslint 0 · unit 2162/2162 · integration 16/16 (scratch DB, all 34 migrations)
PREVIEW: READY — dpl_Bm6pVtHmeteWUDqt25LURohnmyHU @ cd8f2fd (branch alias planner-git-feat-d54-bagtrack-p4-w-fa71b5)

Stack: #507 ← #508 ← #509 ← this PR. The earlier verdicts at their pinned heads stay valid (follow-up PR, no amend).

Clears on: Love's preview sign-off sentence after the re-walk (the whole stack merges together; production applies of 0032–0034 already stand by the Day-54 retroactive acceptance).

## PR #508 — feat(d54): bag-tracking P2 — Asset Tracking + Inventory reports, dark-switch nav, AWB drill-downs (parks)

- Label: `parked-t2`
- Head SHA: `e84ae9bf973493a28d684132460e1f208ff6b685`

ORCH-PARK (Session B, bag-tracking P2 reports)

**Plain English:** stage 2 — the actual screens. Transcorp gets the Asset Tracking report (merchant-by-day rows with per-merchant totals, exactly the columns from your screenshots including Sorted) and a per-merchant Inventory view; merchants get the Inventory report (by-date and by-consignee, consignee rows expand per delivery date). Every number is a link that lands on the tasks list showing exactly the deliveries behind it. The ice-packs and bags meanings you relayed from Aqib are spelled out in the column tooltips verbatim. The whole thing honors the dark switch: merchants see nothing — not even the menu item — until you light them up; the fleet report only shows lit merchants.

**One deliberate simplicity call flagged:** flat nav entries instead of a structural 'Reports' submenu — three links don't justify building a nav-group system. Say the word if you want the visual grouping anyway.

**SQL TO APPLY: no** (stage 1 #507 carries the three parked migrations; this stacks on it). tsc 0 / eslint 0 / unit 2159 / aggregation arithmetic proven against seeded data on the throwaway DB.

**Reviewer:** ORCH-VERDICT APPROVE r1 at pinned head `e84ae9b`.

**Clears on:** your preview sign-off sentence for the whole stack (posture 7a).

## PR #507 — feat(d54): bag-tracking P1 backend — scan log, 5-state enum, 30-min poll, report perms (parks; SQL-TO-APPLY ×3)

- Label: `parked-t2`
- Head SHA: `bc4673be730e575afa5564280943e8d165050679`
- **SQL TO APPLY: yes — supabase/migrations changed. Parks for Love's NAMED authorization; the builder executes and states the route (2026-06-12 ruling).**

ORCH-PARK (Session B, bag-tracking P1 backend)

**Plain English:** the first of the three build stages for the bag-tracking reports you cleared. Everything that has to exist under the reports: the never-overwritten scan-history table, the database's acceptance of the 'Sorted' status (your screenshots showed it; our constraint would have rejected it), the per-merchant on/off switch (OFF for everyone by default — the feature stays invisible until you light a merchant up by sentence), the 30-minute background sweep that keeps the data fresh, and the permission that lets Transcorp staff see across merchants. No screens yet — those are stages 2 and 3.

**SQL TO APPLY: yes — THREE migrations (0032 scan log / 0033 Sorted / 0034 dark switch), each waits for your named go.** Nothing touched the live database; the proof ran on a throwaway local copy with all 34 migrations applied cleanly.

**Cost flag (reviewer Love-trigger #4, also pre-answered by your firing):** the 30-minute sweep uses a QStash schedule. Verified against Upstash's pricing page: free tier allows 1,000 messages/day + 10 schedules; this uses 48/day — $0 at this volume, no new account, same keys already in the stack. The schedule itself is NOT registered yet — that's a one-command step on your go after this merges.

**RED-first:** 11 tests watched failing, then green. tsc 0 / eslint 0 / unit 2146 / integration 8/8 (append-only enforcement, default-OFF switch, Sorted accepted, teardown path).

**Reviewer:** ORCH-VERDICT APPROVE r1 at pinned head `bc4673b`, cost trigger flagged + answered above.

**Clears on:** nothing merges until your preview sign-off sentence (staged posture 7a) — this parks as part of the 3-PR stack; the migrations additionally each need your named SQL go at apply time.

